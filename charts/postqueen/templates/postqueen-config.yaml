apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postqueen.fullname" . }}-config
  labels:
    {{- include "postqueen.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}